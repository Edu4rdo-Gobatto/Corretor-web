VITE_API_URL=/api
API_PROXY_TARGET=http://localhost:3000
VITE_DEMO_MODE=false
# For production, configure a Vercel rewrite from /api to the Render API
# or set VITE_API_URL to the HTTPS API origin and configure its CORS origin.
